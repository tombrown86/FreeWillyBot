# Core (upper bounds reduce pip backtracking)
pandas>=2.0,<3
pyarrow>=10.0  # Required for pd.read_parquet() / pd.to_parquet()
yfinance>=0.2
requests>=2.28

# EUR/USD historical data (Phase 5)
dukascopy-python>=4.0

# Macro (Phase 6/7)
fredapi>=0.5
python-dotenv>=1.0

# News (Phase 8) — GKG raw files via requests (no gdeltdoc)

# Features (Phase 10) — transformers>=4.49 required by chronos-forecasting
transformers>=4.49,<4.52
torch>=2.0,<2.6
sentencepiece>=0.1.99

# Phase 11 — Training (chronos needs scikit-learn>=1.6; >=1.8 requires Python 3.11+)
# If on Python 3.10, use scikit-learn>=1.6,<1.8 and retrain models on that machine
xgboost>=2.0,<2.2
scikit-learn>=1.8,<2  # Python 3.11+ only; for 3.10 use >=1.6,<1.8
# timesfm>=1.0  # Optional: requires Python <3.12; Chronos-Bolt is primary
chronos-forecasting>=2.0

# Phase 15 — Execution (demo only)
oandapyV20>=0.7
python-binance>=1.0
ctrader-open-api>=0.9
service-identity>=24.1  # Twisted TLS hostname verification (quiet get_ctrader_token warning)

# Dashboard
flask>=3.0
